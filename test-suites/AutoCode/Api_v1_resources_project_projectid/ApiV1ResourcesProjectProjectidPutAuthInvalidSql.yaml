---
parent: Api_v1_resources_project_projectid
name: ApiV1ResourcesProjectProjectidPutAuthInvalidSql
description: null
authors:
- apisec Bot
type: SUITE
init: []
localVariables: []
path: /api/v1/resources/project/{projectId}
endpoint: /api/v1/resources/project/{{@Var.projectId}}
method: PUT
auth: Invalid_Auth_SQL
headers:
- 'Accept: application/json'
- 'Content-Type: application/json'
testCases:
- id: 1
  body: |-
    {
      "definition" : "{{@Var.definition}}",
      "dependency" : "{{@Var.dependency}}",
      "endpoints" : [ {
        "coverageStatus" : { },
        "description" : "{{@Random}}",
        "endpoint" : "{{@Random}}",
        "isMapped" : false,
        "manualEndpoint" : false,
        "method" : "{{@Random}}",
        "parameters" : [ {
          "body" : "{{@Random}}",
          "defaultValue" : "{{@Random}}",
          "description" : "{{@Random}}",
          "endpointId" : "{{@Random}}",
          "enums" : "{{@Random}}",
          "example" : "{{@Random}}",
          "format" : "{{@Random}}",
          "in" : "{{@Random}}",
          "name" : "{{@Random}}",
          "required" : false,
          "type" : "{{@Random}}"
        } ],
        "pathDependency" : "{{@Random}}",
        "payload" : "{{@Random}}",
        "projectId" : "{{@Random}}",
        "queryDependency" : "{{@Random}}",
        "resourceName" : "{{@Random}}",
        "responses" : [ {
          "contentType" : "{{@Random}}",
          "endpointId" : "{{@Random}}",
          "ref" : "{{@Random}}",
          "response" : "{{@Random}}",
          "responsePii" : "{{@Random}}",
          "responseStatus" : "{{@Random}}"
        } ],
        "sequenceOrder" : "{{@RandomInteger}}",
        "summary" : "{{@Random}}",
        "tags" : "{{@Random}}",
        "typeThreePath" : "{{@Random}}"
      } ],
      "enumValues" : "{{@Var.enumValues}}",
      "id" : "{{@Var.id}}",
      "inactive" : false,
      "path" : "{{@Var.path}}",
      "projectId" : "{{@Var.projectId}}",
      "resourceName" : "{{@Var.resourceName}}",
      "tags" : "{{@Var.tags}}",
      "typeThreeDefination" : "{{@Var.typeThreeDefination}}",
      "typeThreePath" : "{{@Var.typeThreePath}}"
    }
  inactive: false
useCategoryAssertion: false
assertions:
- '@ResponseTime < 7000'
cleanup: []
severity: Critical
category: InvalidAuthSQL
tags:
- V1
- OWASP-API2
- '[PCI DSS 3.0] 6.5.1'
- OTG-AUTHN-004
- OWASP API Security Top 10
- Broken Authentication
- Non-Intrusive
- Injection
- '[2020 CWE TOP 14] CWE - 287'
- '[MITRE ATT&CK] T1595.002'
inactive: false
policies:
  initExec: Request
  cleanupExec: Request
  logger: DEBUG
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: '@APIsec/Common/auth_invalid_sql'
  repeatDelay: 0
  unmaskToken: true
  postExecutionDelay: 0
  showResponse: true
autoGenerated: true
