---
parent: Api_v1_skills
name: ApiV1SkillsPostBodyParamSsrfExternalUrl
description: null
authors:
- apisec Bot
type: SUITE
init: []
localVariables: []
path: /api/v1/skills
endpoint: /api/v1/skills
method: POST
auth: Default
headers:
- 'Accept: application/json'
- 'Content-Type: application/json'
testCases:
- id: 1
  body: |-
    {
      "accessKey" : "{{@Injection}}",
      "createdBy" : "{{@Var.createdBy}}",
      "createdDate" : "{{@Var.createdDate}}",
      "description" : "{{@Injection}}",
      "host" : "{{@Injection}}",
      "id" : "{{@Var.id}}",
      "inactive" : false,
      "key" : "{{@Injection}}",
      "modifiedBy" : "{{@Var.modifiedBy}}",
      "modifiedDate" : "{{@Var.modifiedDate}}",
      "name" : "{{@Injection}}",
      "opts" : [ {
        "id" : "",
        "label" : "{{@Random}}",
        "mandatory" : false,
        "value" : "{{@Random}}"
      } ],
      "org" : {
        "createdBy" : "{{@Var.createdBy}}",
        "createdDate" : "{{@Var.createdDate}}",
        "id" : "{{@Var.id}}",
        "inactive" : false,
        "modifiedBy" : "{{@Var.modifiedBy}}",
        "modifiedDate" : "{{@Var.modifiedDate}}",
        "name" : "{{@Injection}}",
        "orgPlan" : "{{@Injection}}",
        "version" : "{{@Var.version}}"
      },
      "prop1" : "{{@Injection}}",
      "prop2" : "{{@Injection}}",
      "prop3" : "{{@Injection}}",
      "prop4" : "{{@Injection}}",
      "prop5" : "{{@Injection}}",
      "secretKey" : "{{@Injection}}",
      "skillType" : "{{@Var.skillType}}",
      "version" : "{{@Var.version}}"
    }
  inactive: false
useCategoryAssertion: false
assertions:
- '@Response !=~ (?s:.)*<(?s:.*)(APISec External SSRF)(?s:.*)>(?s:.*)'
- '@ResponseTime < 7000 OR @StatusCode == 400'
cleanup: []
severity: High
category: ssrf_external_url
tags:
- V1
- OWASP-API8
- OWASP API Security Top 10
- SSRF
inactive: false
policies:
  initExec: Request
  cleanupExec: Request
  logger: DEBUG
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: '@APIsec/Common/ssrf_external_url'
  repeatDelay: 0
  unmaskToken: false
  postExecutionDelay: 0
  showResponse: true
autoGenerated: true
